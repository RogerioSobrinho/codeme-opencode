---
description: Decompose a complex task into independent subtasks and coordinate multiple specialized agents in parallel.
agent: orchestrator
---

Orchestrate the following complex task using multiple specialized agents:

$ARGUMENTS

Steps:
1. Scan the project briefly to understand context
2. Analyze and decompose into independent and dependent subtasks
3. Present an orchestration plan with:
   - Which subtasks run in parallel (truly independent)
   - Which subtasks run sequentially (dependent)
   - Which agent handles each subtask
4. Wait for confirmation before executing
5. Execute: launch parallel subagents simultaneously, sequential ones in order
6. Synthesize results — report what each agent produced and any conflicts found
